apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perfectpick-ingress
  namespace: perfectpick
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: "34.14.203.32.nip.io"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: perfectpick-service
            port:
              number: 80